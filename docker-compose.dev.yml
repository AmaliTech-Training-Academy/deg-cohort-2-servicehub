version: '3.9'

services:
  backend:
    environment:
      - SPRING_PROFILES_ACTIVE=dev

  db:
    ports:
      - "5432:5432"

  adminer:
    image: adminer:latest
    restart: unless-stopped
    ports:
      - "8081:8080"
    networks:
      - app-network
